{
  "$schema": "../../../schemas/conformance-vector.schema.json",
  "id": "protocol.decode-payment-required.oversized-header",
  "kind": "protocol.decode-payment-required",
  "milestone": "M1",
  "title": "A challenge larger than 64 KiB decoded is rejected before JSON parsing",
  "description": "The header is generated by both runners from a compact deterministic recipe. Committing roughly 87 KiB of base64 would obscure the actual boundary under test (SEC-006).",
  "spec": ["SPEC §6.2", "SEC-006"],
  "input": {
    "requestUrl": "https://api.example.com/v1/completions",
    "requestMethod": "POST",
    "generatedHeader": { "kind": "repeated-ascii", "decodedBytes": 65537 },
    "clockEpochMs": 1785711360000
  },
  "expected": {
    "outcome": "invalid",
    "errorCode": "TX402_PAYMENT_REQUIRED_INVALID",
    "reason": "header-too-large"
  }
}
